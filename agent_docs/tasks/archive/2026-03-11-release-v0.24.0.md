# 2026-03-11-release-v0.24.0.md

## Issue
Prepare and finalize the v0.24.0 release of Sovereign Watch.

## Solution
Finalized the H3 Poller visualization, integrated System Settings UI, fixed AIS poller issues, and resolved all remaining frontend linting errors including a critical `NaN` coordinate hotfix.

## Changes
- **Core**: Version bump to `v0.24.0`.
- **UI**: Added System Settings Widget and TopBar toggle button.
- **Map**: Optimized H3 mesh rendering (occlusion fixes, opacity adjustments).
- **JS8Call**: Robust `NaN` coordinate handling and improved `KiwiNodeBrowser` stability.
- **Backend/Ingestion**: AIS and H3 poller improvements.
- **Maintenance**: Zero lint errors in frontend.

## Verification
- Verified clean build: `docker compose build frontend` (SUCCESS).
- Verified linting: `npm run lint` (inside container) (SUCCESS).
- Manual verification of map rendering and UI toggles.

## Benefits
- Improved system configurability through the new Settings UI.
- Enhanced map clarity with H3 mesh optimization.
- Increased stability through robust coordinate handling and lint resolution.
